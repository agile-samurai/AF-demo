{
  "name": "Functional-Tests",
  "description": "Functional Tests",
  "scripts": {
    "lint": "tslint \"tests/**\" \"page-objects/**\"",
    "node_modules:clean": "rm -rf node_modules && npm install",
    "protractor": "npm run lint && protractor protractor.config.js",
    "webdriver:start": "webdriver-manager start --standalone",
    "webdriver:update": "webdriver-manager update --standalone"
  },
  "devDependencies": {
    "@types/jasmine": "2.8.9",
    "jasmine-spec-reporter": "^4.2.1",
    "protractor": "5.3.0",
    "ts-node": "3.2.0",
    "tslint": "^5.12.0",
    "typescript": "2.4.2",
    "protractor-http-client": "1.0.4"
  },
  "engines": {
    "node": ">= 10.16.0",
    "npm": ">= 6.9.0"
  }
}
