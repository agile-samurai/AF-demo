from setuptools import setup

setup(
    name="rdso",
    version="1.0",
    description="Python module for data science services for RDSO submission",
    author=["Brian Austin", "David Waterman"],
    author_email="datascienceu@u.group",
    packages=["rdso"],  # same as name
)
